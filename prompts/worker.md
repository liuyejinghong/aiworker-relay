# Worker Prompt Draft

Status: inactive. No runtime code loads this file.

When a concrete worker contract is accepted, the prompt must carry the agreed task fields from docs/worker-contract.md and must not grant scope beyond the task contract.
